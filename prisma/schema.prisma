generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model TimeEntry {
  id          String   @id @default(cuid())
  date        DateTime
  project     String
  hours       Float
  description String
  createdAt   DateTime @default(now())
}